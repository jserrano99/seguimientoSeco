{% extends "base.html.twig" %}
{% block title %} CONSULTA AGRUPACIONES {% endblock %}

{% block titulo %} 
    <div id='dtitulo' class="container"> AGRUPACIONES **CONSULTA**
    <a href="{{path("addAgrupacion")}}"
       class="btn btn-success">
        <span class="glyphicon glyphicon-new-window"></span> Nueva Agrupación</a>
    <hr/>
    </div>
{% endblock %} 

{% block contenido %}
    <div class="container-fluid">
        {% for message in app.session.flashbag().get('status') %}
            <div class="alert alert-success">
                {{message}}
            </div>
            <hr/>
        {% endfor %}
        {{ sg_datatables_render(datatable) }}
    </div>
{% endblock %}
